---
title: Récolte de bois régionale
toc: false
sql:
  recolte_regions_historique: ./data/recolte_regions_historique.csv
---

<!-- ----------------------------------------------------------------------------------------------------------------------- 
                                     Début du code pour la présentation de la page
------------------------------------------------------------------------------------------------------------------------ -->

<link rel="stylesheet" href="./style.css">

<div style = "background-color: #f0f4f8;border: 2px solid #d1d9e6;border-radius: 10px;text-align: right;padding: 23px;font-family: Marianne, sans-serif;font-size: 20px;font-weight: bold;color: #333;text-decoration: none;transition: transform 0.2s ease, box-shadow 0.2s ease;width: 80%;">
  <h2> Récolte de bois régionale </h2>
</div>

<h3><br>Choix des paramètres : </h3>

```js
/*Affichage des 3 listes de paramètres dynamiques*/ 
const choix_liste_regions1 = view(Inputs.select(noms_regions, { label: "Choississez une région :"}));
const choix_liste_essences1 = view(Inputs.select(noms_essences, { label: "Choisissez une essence :"}));
const choix_liste_annee1 = view(Inputs.select(annees, { label: "Choisissez une année :", value: annees.sort().reverse()[0]}));
```

---

<h3>Résumé</h3><br>

<h4>En ${choix_liste_annee1}, ${chiffre_cle_volume_annee.toFixed(0)} milliers de m3 ronds sur écorce de "${choix_liste_essences1}" ont été récoltés dans la région "${choix_liste_regions1}".</h4>

---

<h3>Répartition de la récolte régionale en ${choix_liste_annee1} dans la région "${choix_liste_regions1}" : </h3>

<div style="width: 2000px; height: 530px; overflow: hidden; border: 0px solid #999999;">
<br>
  <!--${treemap_recolte_reg}-->
  <br>
  <div style="width: 5000px; height: 45px; overflow: hidden; border: 0px solid #999999;font-size: 14px;margin-bottom: 0 ">
  <i>Graphique 1 : Répartition de la récolte régionale en ${choix_liste_annee1} dans la région "${choix_liste_regions1}".<br> Source : Enquête EXF-SRI</i>
  </div>
</div>

---

<h3>Evolution de la récolte régionale historique de "${choix_liste_essences1}" : </h3>

<br>(Optionnel) Vous pouvez choisir une seconde région à comparer :

```js
/*Affichage des listes de paramètres dynamiques*/ 
const choix_liste_regions2 = view(Inputs.select(noms_regions));
```

<div style="width: 2000px; height: 600px; overflow: hidden; border: 0px solid #999999;">
  ${graph_recolte_reg_hist}
  <div style="width: 5000px; height: 45px; overflow: hidden; border: 0px solid #999999;font-size: 14px;margin-bottom: 0 ">
  <i>Graphique 2 : Evolution de la récolte régionale historique de  "${choix_liste_essences1}".<br> Source : Enquête EXF-SRI</i>
  </div>
</div>

<!-- ----------------------------------------------------------------------------------------------------------------------- 
                                     Fin du code pour la présentation de la page
------------------------------------------------------------------------------------------------------------------------ -->

<!-- ----------------------------------------------------------------------------------------------------------------------- 
                                     Début du code pour réaliser les graphiques
------------------------------------------------------------------------------------------------------------------------ -->

```js
import { select, table } from "@observablehq/inputs";
import {createOptionsEChartsFromData} from "./components/graphHisto.js";
import {Treemap} from "./data/fonctions.js"
```

```sql id=liste_regions
/*Requête pour obtenir la liste des départements*/ 
SELECT DISTINCT Région as regions
FROM recolte_regions_historique
```

```sql id=liste_essences
/*Requête pour obtenir la liste des essences*/ 
SELECT DISTINCT Récolte as essences 
FROM recolte_regions_historique
```

```sql id=borne_annees
/*Requête pour obtenir la liste des années*/ 
SELECT 
  MIN(CAST(column_name AS INTEGER)) AS annee_min,
  MAX(CAST(column_name AS INTEGER)) AS annee_max
FROM (
  SELECT column_name
  FROM information_schema.columns
  WHERE table_name='recolte_regions_historique'
  AND column_name LIKE '____' -- 4 caractères
)
```

```js
/*Création de la liste des années à partir des bornes obtenues dans la requête*/ 
const { annee_min, annee_max } = borne_annees.toArray()[0];
const annees = Array.from(
  { length: annee_max - annee_min + 1 }, 
  (_, i) => (annee_min + i).toString()
);
```

```js
/*Mise en forme*/ 
const tab_regions = liste_regions.toArray();
const tab_essences = liste_essences.toArray();
```

```js
/*Mise en forme*/ 
const noms_regions = tab_regions.map(d => d.regions);
const noms_essences = tab_essences.map(d => d.essences);
```

<!-- ------------------------------
Chargement des données CSV
-------------------------------- -->

```js
const fichier_recolte_init = await FileAttachment("./data/recolte_regions_historique.csv").dsv({delimiter: ";", array: true})
const colonnes_fichier_recolte_init = fichier_recolte_init[0];
const lignes_fichier_recolte_init = fichier_recolte_init.slice(1);

const data_recolte = lignes_fichier_recolte_init.map(lignes_fichier_recolte_init => {
  const obj = {};
  colonnes_fichier_recolte_init.forEach((colonnes_fichier_recolte_init, index) => {
    obj[colonnes_fichier_recolte_init] = lignes_fichier_recolte_init[index];
  });
  return obj;
});
```

<!-- -------------------------------------------------------------------------------------------------------------------- -->
<!-- ------------------------------
Chiffre clé du résumé
-------------------------------- -->

```sql id=data_chiffre_cle_recolte
SELECT * 
FROM recolte_regions_historique 
WHERE Région=${choix_liste_regions1} 
AND Récolte=${choix_liste_essences1}
```

```js
const data_chiffre_cle_recolte_filtree = data_chiffre_cle_recolte.toArray();
const row_data_chiffre_cle_recolte_filtree = data_chiffre_cle_recolte_filtree[0];

const data_chiffre_cle_recolte_filtree_final = data_evol_recolte_reg1_filtree.flatMap(row_data_chiffre_cle_recolte_filtree => {
  const years1 = Object.keys(row_data_chiffre_cle_recolte_filtree).filter(d => /^\d{4}$/.test(d));
  return years1.map(y => ({
    Region: row_data_chiffre_cle_recolte_filtree.Région,
    Annee: +y,
    Volume: parseFloat(row_data_chiffre_cle_recolte_filtree[y].replace(",", ".")),
    Recolte: row_data_chiffre_cle_recolte_filtree.Récolte
  }));
});

const chiffre_cle_volume_annee = data_chiffre_cle_recolte_filtree_final.find(item => item.Annee === Number(choix_liste_annee1))?.Volume;
```

<!-- -------------------------------------------------------------------------------------------------------------------- -->
<!-- ------------------------------
Treemap récolte régionale
-------------------------------- -->





<!-- -------------------------------------------------------------------------------------------------------------------- -->
<!-- ---------------------------------------
Graphique évolution récolte départementale
---------------------------------------- -->

```sql id=data_evol_recolte_reg1
SELECT * 
FROM recolte_regions_historique 
WHERE Région=${choix_liste_regions1} 
AND Récolte=${choix_liste_essences1}
```

```sql id=data_evol_recolte_reg2
SELECT * 
FROM recolte_regions_historique 
WHERE Région=${choix_liste_regions2} 
AND Récolte=${choix_liste_essences1}
```

```js
const data_evol_recolte_reg1_filtree = data_evol_recolte_reg1.toArray();
const data_evol_recolte_reg2_filtree = data_evol_recolte_reg2.toArray();
```

```js
// On récupère la première ligne
const row_data_evol_recolte_reg1_filtree = data_evol_recolte_reg1_filtree[0];
const row_data_evol_recolte_reg2_filtree = data_evol_recolte_reg2_filtree[0];
```

```js
// On crée un tableau "long" {Region, Annee, Volume}
const data_evol_recolte_reg1_filtree_final = data_evol_recolte_reg1_filtree.flatMap(row_data_evol_recolte_reg1_filtree => {
  const years1 = Object.keys(row_data_evol_recolte_reg1_filtree).filter(d => /^\d{4}$/.test(d));
  return years1.map(y => ({
    Region: row_data_evol_recolte_reg1_filtree.Région,
    Annee: +y,
    Volume: parseFloat(row_data_evol_recolte_reg1_filtree[y].replace(",", ".")),
    Recolte: row_data_evol_recolte_reg1_filtree.Récolte
  }));
});

const data_evol_recolte_reg2_filtree_final = data_evol_recolte_reg2_filtree.flatMap(row_data_evol_recolte_reg2_filtree => {
  const years2 = Object.keys(row_data_evol_recolte_reg2_filtree).filter(d => /^\d{4}$/.test(d));
  return years2.map(y => ({
    Region: row_data_evol_recolte_reg2_filtree.Région,
    Annee: +y,
    Volume: parseFloat(row_data_evol_recolte_reg2_filtree[y].replace(",", ".")),
    Recolte: row_data_evol_recolte_reg2_filtree.Récolte
  }));
});
```

```js
const maxVolume_data_evol_recolte_reg1_filtree = Math.max(...data_evol_recolte_reg1_filtree_final.map(d => d.Volume));
const maxVolume_data_evol_recolte_reg2_filtree = Math.max(...data_evol_recolte_reg2_filtree_final.map(d => d.Volume));
const maxVolume = Math.max(maxVolume_data_evol_recolte_reg1_filtree, maxVolume_data_evol_recolte_reg2_filtree);
```

```js
const graph_recolte_reg_hist = Plot.plot({
  width: 800,
  height: 400,
  x: {
    label: "Année",
    tickFormat: d => d.toString(), // texte pour l'axe X
  },
  y: {
    label: "Volume (en milliers de m3 ronds sur écorce)",
    domain: [0, maxVolume * 1.1]   // axe Y à partir de 0
  },
  color: {legend: true}, // légende pour différencier les départements
  marks: [
    // première courbe
    Plot.line(data_evol_recolte_reg1_filtree_final, {x: "Annee", y: "Volume", stroke: "Region"}),
    Plot.dot(data_evol_recolte_reg1_filtree_final, {x: "Annee", y: "Volume", fill: "Region", r:5,
                     title: d => `${d.Region}\n${d.Annee}: ${d.Volume.toFixed(0)} milliers de m3 ronds sur écorce de ${d.Recolte}`}),
    
    // deuxième courbe
    Plot.line(data_evol_recolte_reg2_filtree_final, {x: "Annee", y: "Volume", stroke: "Region"}),
    Plot.dot(data_evol_recolte_reg2_filtree_final, {x: "Annee", y: "Volume", fill: "Region", r:5,
                     title: d => `${d.Region}\n${d.Annee}: ${d.Volume.toFixed(0)} milliers de m3 ronds sur écorce de ${d.Recolte}`})
  ]
})
```
